# /public/uploads

Drop the walking-character video here:

- `Dress_walking_animation_202604132210.mp4`

Optional (recommended for perf): provide a WebM version alongside the MP4:

- `Dress_walking_animation_202604132210.webm`

The hero component (`components/hero/ScrollHero.tsx`) will auto-load whichever
is present via the `<source>` fallback chain.
